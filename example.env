# Where your server can be reached from the outside for IMAP/POP/SMTP/Webmail.
# Can be multiple separated by a comma (Traefik Host/HostSNI rules).
HOSTNAMES=mail.example.com

# External hostnames used by services like autodiscover/autoconfig.
MAIL_DOMAIN=example.com
MAIL_HOSTNAME=mail.example.com
COMPANY_NAME=Example Mail
SUPPORT_URL=https://example.com/support

# Bind ports to a specific IP (optional). Defaults to 0.0.0.0 if empty.
# PUBLIC_IP=203.0.113.10

# WildDuck/ZoneMTA/Haraka build sources (optional overrides)
# WILDDUCK_REPO=https://github.com/nodemailer/wildduck.git
# WILDDUCK_REF=master
# ZONEMTA_TEMPLATE_REPO=https://github.com/zone-eu/zone-mta-template.git
# ZONEMTA_TEMPLATE_REF=master
# ZONEMTA_VERSION=latest
# ZONEMTA_WILDDUCK_VERSION=latest
# HARAKA_REPO=https://github.com/haraka/Haraka.git
# HARAKA_REF=master
# HARAKA_PLUGIN_VERSION=latest

# Traefik/ACME cert sync settings (Haraka + ZoneMTA STARTTLS)
CERT_DOMAIN=mail.example.com
# CERT_RESOLVER=letsencrypt
# ACME_PATH=/traefik/acme.json

# Auto-archive defaults (per-user settings can override)
# ARCHIVE_BASE=Archives
# ARCHIVE_MONTHS=3
# ARCHIVE_PATTERN={base}/{year}/{month}
# ARCHIVE_ALL_FOLDERS=
# ARCHIVE_INCLUDE_BASE=
# ARCHIVE_USERS=
# ARCHIVE_PROGRESS=
# ARCHIVE_SCHEDULE=15 2 * * *

# Used as the Let's Encrypt contact address for expiry notices:
# https://letsencrypt.org/docs/expiration-emails/
# ACME_EMAIL=admin@example.com

# Graylog (only needed with docker-compose.observability.yml)
# GRAYLOG_PASSWORD_SECRET=replace-with-strong-secret
# GRAYLOG_ROOT_PASSWORD_SHA2=replace-with-sha256-of-admin-password
# OPENSEARCH_JAVA_OPTS=-Xms1024m -Xmx1024m
